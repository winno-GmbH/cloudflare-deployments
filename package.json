{
  "name": "form-tool-project",
  "version": "1.0.0",
  "description": "Form tool project with TypeScript compilation workflow",
  "scripts": {
    "build": "cd dev/form-tool/form-tool-v2 && npm run build",
    "install-deps": "cd dev/form-tool/form-tool-v2 && npm install && npm install fs-extra"
  },
  "dependencies": {
    "fs-extra": "^11.1.1"
  }
}
